// Shared, language-independent site constants.

// The single main conversion path (see docs/REDESIGN.md, D1): every primary CTA
// on the site — hero, header, contact card — points here. A call has a lower
// threshold than writing an e-mail, so e-mail stays the secondary action.
export const CALENDLY_URL = 'https://calendly.com/dawid-adamski-redpulse/30min';
